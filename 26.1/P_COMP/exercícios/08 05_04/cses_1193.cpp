#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n, m;
	cin >> n >> m;

  vector<string> map (n);
	vector<vector<int>> graph;
  pair<int, int> a, b;

	for (int i = 0; i < n; i++) {
		cin >> map[i];
  }

  for (int i = 0; i < n; i++) {
		for (int j = 0; j < m; j++) {
			switch (line[j]) {
			case '#':
				break;
			
			case '.':
			  if (i != 0) {
			    if () {
			      
			    }
			  }
				break;
			
			case 'A':
				break;
      
      case 'B':
        break;
			}

		}
	}
}